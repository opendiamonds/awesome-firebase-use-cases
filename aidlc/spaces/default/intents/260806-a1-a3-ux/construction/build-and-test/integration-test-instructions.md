# Integration Test Instructions

## Strategy Note

**Minimal** test strategy：本 intent **略過**新增整合測試套件。

## Upstream

參見 `code-summary.md` — HTTP 層 prompt_guard 整合測試列為 reviewer Minor，非本期必交。

## Optional Follow-up

若升級至 Standard：以 FastAPI TestClient 覆蓋 `/generate` 拒答 SSE。
