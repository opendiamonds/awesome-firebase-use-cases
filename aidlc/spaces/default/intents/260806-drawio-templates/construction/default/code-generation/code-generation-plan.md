# Code Generation Plan

## Traceability
- **Requirement**: Update the Azure geometry example in the system prompt (`cloud_architecture_system_prompt.md`) to align with the new `Azure_template.drawio.xml` coordinates. GCP remains unchanged.

## Steps
- [x] Step 1: Update the Azure geometry template and JSON example in `backend/prompts/cloud_architecture_system_prompt.md` to match coordinates in `backend/prompts/Azure_template.drawio.xml`. (Completed)
- [x] Step 2: Validate the repository contract using `validate_repo_contract.py`. (Completed)
