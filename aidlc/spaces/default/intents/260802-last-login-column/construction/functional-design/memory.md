<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
- 2026-08-10T09:00:00Z — **[U2]** reviewer 抓到一個 Critical：我把「三個端點都帶出兩欄」的保護錯歸給欄位集合斷言。實測三個端點皆宣告 `response_model`，**回應 key 集合由回應模型的欄位宣告決定，與構造點傳了什麼無關** —— 有預設值的欄位不論傳不傳都會出現。所以 S2 的既有缺陷從來不是「少了欄位」，是**值被靜默預設為 `null`**。真正的分工是：R1 的必填宣告擋構造遺漏（結構性、非測試）、AC-1.5 的**值斷言**擋靜默預設、欄位集合斷言擋 schema 層增刪。三者互補。
- 2026-08-10T09:00:00Z — **[U2]** 更難堪的是**上游早就寫對了**：AC-1.5 的 Then 逐字寫「帶有與資料庫一致的**值**……而非因構造遺漏而缺失**或為 `null`**」—— 那個「或為 null」正是 `response_model` 的行為。上游預見了這條失敗路徑，我在轉譯成驗證強度表時把它丟了，一列值斷言都沒規劃。教訓：**AC 的 Then 子句要逐字拆解到驗證項，不能概括** —— 「或為 null」這四個字就是整個 Critical 的來源。
- 2026-08-09T16:00:00Z — **[U3]** 出 Q1 時把「沿用既有實務」寫成「無參數 `toLocaleString()`」，但上游 `refined-mockups-questions.md` 前言已把**時間格式 `YYYY-MM-DD HH:MM`** 列為「已由上游定案、不重問」，而無參數呼叫產出的是 locale 相依格式。拆開後兩者其實正交：**格式**由上游定死，**時區政策**才是本題真正在問的。依 U1 Q3 的既有先例以 Revision 段記錄、原答案不改寫、上游不回改。教訓：寫選項時引用「既有實務」要連它的**輸出形狀**一起核對，不能只核對它的呼叫方式。
- 2026-08-09T16:00:00Z — **[U3]** e2e 時間斷言的不確定性來源不是顯示選擇，而是 **Playwright config 未釘住 locale 與 timezone**。修在測試層（釘住 config）而非產品層（改成固定 UTC）—— 為了遷就測試而改產品行為是把問題修在錯的地方。
- 2026-08-09T15:00:00Z — **[U4]** reviewer 抓到我把上游 iteration 4 已標記的殘留項 **M1-a** 原樣抄下卻沒標出死角：更新謂詞（最後異動者為 NULL／系統識別字）比契約句（尚未被本補丁套用過）窄一格，差在「該列在部署前已被管理員動過」這個**可達**狀態。實測管理員的更新分支是**整列賦值**，所以連調 `can_edit` 都會留下管理員帳號 → 補丁永不套用 → US-3 在正是它為之而生的環境上靜默落空。教訓：承接上游殘留清單時，逐項核對「這一項在我的 artifact 裡落在哪」，沒落點就是漏接。
- 2026-08-09T15:00:00Z — **[U4]** 把上游要求的**三態**記錄拆為**四態**。上游把「已套用過」與「已被管理員調整」合為一格「已跳過」，而死角恰好落在後者 —— 合併等於把新加的觀測點對準它最該照亮的地方關掉。這不是推翻上游，是承接 M1-a 的建議修正。
- 2026-08-09T14:00:00Z — **[U2]** C-4 把「不設預設值」與「共用工廠函式」並列為對等的二擇一，**查證後兩者並不對稱**：清單端點的 `requested_role` 需要一次額外 DB 查詢且只在待授權時執行，另兩處完全不傳 —— C-4 自己寫的 `(user, now)` 簽章重現不了這個差異。不讀 code 時兩個選項看起來勢均力敵，讀了才發現其中一個的前提不成立。
- 2026-08-09T14:00:00Z — **[U2]** 工廠函式還有一個上游沒點出的代價：它會**順手碰到已被 phase-check 劃為範圍外的既有缺陷**（兩個 PUT 端點漏傳 `requested_role`），必須就地決定補或不補。補了超範圍、不補則工廠內部仍要分歧。這使選項二在本 intent 的具體脈絡下比抽象比較時更貴。
- 2026-08-09T13:00:00Z — **[U4]** 逐案推演後確認：在「只更新不插入」成立的前提下，**執行順序對正確性其實是冗餘的**（空表→補丁更新 0 列、種子以新預設值寫入；非空表→種子本來就直接返回；兩種排法都正確）。上游把兩者並列為契約沒有錯，但因果要講清楚才知道哪一項絕對不能動：**災難來自「插入」這個動作本身，與它排在哪裡無關**。兩項仍照做，並在 artifact 中把這個推演寫出來。
- 2026-08-09T13:00:00Z — **[U4]** 執行順序另有一個獨立且成立的理由，比上游給的更好：放在種子**之後**，目標列必然存在，「未命中目標列」才是真訊號；放在之前則每個全新環境都會在空表上誤報一次。R4 的三態記錄是本單元唯一的驗證手段，一個每次都誤報的訊號等於沒有訊號。
- 2026-08-09T13:00:00Z — **[U4]** Q2=A 的決定性依據是實測而非偏好：`init_db()` 整段包在 `except Exception → rollback` 且**不重拋**、`finally` 會 `db.close()`。沿用同一 session 而忘記提交 → 變更被靜默丟棄（risk log 的高嚴重度項）；且三態記錄會被一句籠統的「初始化時發生錯誤」蓋掉。放在 try/finally 之後同時解決順序、提交、可觀察性三件事。
- 2026-08-09T12:10:00Z — **[U1]** 新增 R0「什麼算一次活動」（reviewer Finding 2）。上游只說「任何以有效憑證發出的請求」，沒說在認證流程的哪一點算數，而 `auth.py:60-64` 的停用 403 就落在觸發點前後一行之間：放在檢查**之前**，已停用帳號只要持舊 token 輪詢就永遠不會被標為逾期（對稽核是直接的錯誤訊號）；放在**之後**才在停用當下凍結。上游 `component-methods.md` C-2 的「尾端」二字本來就編碼了這個決定，是我在主流程丟失了那個位置精度。
- 2026-08-09T12:10:00Z — **[U1]** R0 判定 `authorization_status` 非 approved 的請求**記為活動**：該檢查在更後面的權限層、不在認證依賴內，此類帳號確實通過了認證。待授權與已停用帳號都會出現在清單（`list_users` 回傳全部使用者），但 FR-4.2 已定案不做欄位級控制、refined-mockups 已為該狀態設計呈現，故為已知可接受而非缺陷。
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-08-09T11:00:00Z — **[U1]** 本單元的多數功能決策已由 application-design 定死（門檻值、邊界方向、交易語意、時區契約、欄位可為空且無預設值），故本站的問題只問**實作落點**層級的未定項，不重開已核可的設計決定。
- 2026-08-09T11:00:00Z — **[U1]** `frontend-components` 不產出：stage 檔的 `produces_kinds` 將其限定於 `ui` kind，而本單元為 `service`。引擎的 produces 清單已反映此點（只列三份）。

## Deviations
- 2026-08-10T10:00:00Z — **[U3]** reviewer 抓到我**發明了一個不存在的缺口**：宣稱純 CSS 斷點讓兩份標記並存、必須管理 `aria-hidden` 且「工具鏈不會發現不一致」。實際上切換用的 `display: none`（Tailwind `hidden`／`md:hidden`）**原生就把子樹排除在無障礙樹外** —— 視覺隱藏與無障礙排除是同一條 CSS 規則，沒有需要同步的屬性。更糟的是若開發者照字面去用 JS 動態設該屬性，會撞回 Q3 本來要規避的 error 級 lint 規則。且我沒核對 `accessibility-checklist.md`，該關切上游早有 **R-2** 在追蹤。教訓：**宣告「本站新引入的缺口」前，先確認機制上它是否真的存在、以及上游是否已在追蹤** —— 過度謹慎產生的假警報會誤導實作走上錯路。
- 2026-08-10T09:30:00Z — **[U2]** reviewer iteration 2 抓到我修正時新引入的 Major：Q3 的 Revision 段宣稱選項描述「已更正」，**但我實際上沒編輯選項本文**，A／B 仍逐字保留被 Revision 自己判定不成立的句子 —— 等於把 iteration 1 要求消除的矛盾換個位置留著。這是同型錯誤在本 stage 的**第三次**（U4 的三態→四態序數未同步、本題兩次）。處置：Revision 的自述改為誠實（「選項本文不改寫，但不成立的句子已就地標註」），並在 A／B 的該句加刪除線與更正說明 —— 既不回改已作答紀錄，也不留矛盾。
- 2026-08-09T16:30:00Z — **[U3]** 初版依前三個單元的形狀產出 `business-rules.md` 與 `domain-entities.md`，**錯誤**。stage 檔的 `produces_kinds` 明訂：`business-rules`／`domain-entities` 僅適用 service／spec／library，`frontend-components` 才是 **ui** 類單元的必要產出。已重組進 `frontend-components.md` 並移除那兩份。教訓：per-unit stage 的產出集合**隨 unit kind 變動**，不能沿用前一個單元的形狀 —— directive 的 `produces` 欄每次都要看。
- 2026-08-09T16:30:00Z — **[U5]** `api-type-contract` 的 `kind: packaging` **不在任何一個 `produces_kinds` 清單中**，故本 stage 對它不產出任何 artifact，引擎從未派工是正確行為、非缺口。已核對五個單元的產出集合全部符合 `produces_kinds`。
- 2026-08-09T12:10:00Z — **[U1]** 時區正規化 helper 由「私有」改為**本單元的公開介面**（reviewer iteration 1 Finding 1）。原設計說「呼叫端不需要知道時區問題的存在」，但本單元的兩個判定回傳 bool 不回傳值，U2 的序列化前正規化結構上取不到 —— 私有的代價是 U2 必然自寫第二份可漂移副本，違反 `team.md` 單一真實來源。選擇公開（reviewer 選項①）而非明記「U2 自行實作」（選項②），因為後者要另外附一致性測試義務，成本更高且副本仍存在。
- 2026-08-09T12:10:00Z — **[U1]** `domain-entities.md` 明記刻意偏離 requirements C-5 的「資料庫層預設值」部分（Finding 8）。C-5 原文與「可為空且無預設值」不相容；判定其真實意圖是「新增欄位不得使既有列不合法」，可為空已滿足。偏離標明而非默默照做。

## Deviations（原有）
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->

## Tradeoffs
- 2026-08-10T09:00:00Z — **[U2]** Q1／Q3 的**定案都不變**，改的是依據。Q1 的「工廠拿不到 C-4 的簽章」被檢驗為過度延伸（可把查詢結果掛動態屬性繞過），已縮為「需要繞道而非不可能」；A 仍正確，但靠的是理由②③。Q3 的「完全相等」仍選，但作用範圍由「防構造遺漏」更正為「防 schema 層增刪」。**兩題都以 Revision 段記錄、原答案不改寫** —— 決定對、理由錯，就只修理由。
- 2026-08-09T16:00:00Z — **[U3]** Q3 選純 CSS 斷點不是風格偏好：JS 版斷點的初始量測必須在 effect 內 setState，直接違反 error 級的 `react-hooks/set-state-in-effect`，CI 會紅；且全前端零 `matchMedia`／`innerWidth`，無先例可循。代價（兩份標記並存、需 `aria-hidden`）已內化為 R3 的規則本文，不是可選加強。
- 2026-08-09T16:00:00Z — **[U3]** Q2 選新增桌面 e2e 而非人工承接。成本論證是實測來的：既有 e2e **無一導覽至 Admin 頁**，本單元本來就要寫 repo 第一個 Admin 頁 e2e，涵蓋三項操作是在同一支檔案多寫幾個 case，不是新建機制。若不知道這件事，B3「已有 AC-2.2 靠人工」看起來會像是接受第二項人工的理由。
- 2026-08-09T15:00:00Z — **[U4]** 死角**不改寬謂詞**來解決：改寬會覆寫管理員的既有調整，與 AD-7 否決替代方案 B 的理由直接衝突。改以「四態告警 + 部署後人工核對」承接，並誠實寫明在這條路徑上「不依賴人工步驟即自動生效」的承諾不成立 —— 寧可記下承諾的邊界，也不為了讓承諾看起來完整而改壞設計。
- 2026-08-09T14:00:00Z — **[U2]** 選項一的代價如實寫進 artifact，不掩蓋：它保證「漏傳會失敗」而非「不可能漏傳」，將來第四個構造點仍靠人記得。選項二在這點確實更強。判準是**代價的確定性**：選項二的兩項代價具體且立即，選項一的代價是假設性的。
- 2026-08-09T14:00:00Z — **[U2]** Q3 選完全相等而非包含式斷言，代價是日後合理新增欄位時測試會紅。接受它的理由是實證的：這三個端點**已經真的發生過**「新增欄位時漏掉既有欄位」，而包含式斷言抓不到那件事。
- 2026-08-09T13:00:00Z — **[U4]** Q1 選全量 308 列比對而非單列：實測兩份來源都是可機器解析的字面值，**全量比對只比單列多一行 regex**。查證前兩個選項看起來成本差很多，查證後差距消失，於是「只鎖 1/308」變成沒有理由的選擇。這個檔的「勿手改；改 SQL 後重跑產生腳本」契約已因產生腳本不存在而失效，全量比對是目前唯一能鎖住它的機制。
- 2026-08-09T13:00:00Z — **[U4]** Q3 選「值已正確就不寫標記」，代價是每次啟動多一次單列主鍵 SELECT。換到的是稽核跡象的誠實性（全新環境那一列確實是種子寫的，不該被標成補丁改的）與不無謂推進 `updated_at`（模型帶 `onupdate`）。冪等性不受影響。
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

## Open questions
- 2026-08-09T16:00:00Z — **[U3]** 純 CSS 方案**新引入**一個缺口：`aria-hidden` 與斷點條件若不一致，螢幕閱讀器會讀到重複或錯誤內容，而現行工具鏈不會發現。已在 artifact 的驗證強度表如實標為「無自動化驗證」並註明是本站引入的，不是繼承自上游。
- 2026-08-09T15:00:00Z — **[U4]** 承接 m5：補丁識別字與使用者帳號共用同一欄位，而帳號無格式約束，理論上可碰撞；且本形狀為**每列單次使用**，日後第二支補丁動到同一列時不得沿用同一謂詞。具體識別字字串（須含空白或冒號等不可能成為帳號的字元）留實作階段。
- 2026-08-09T14:00:00Z — **[U2]** 兩個 PUT 端點漏傳 `requested_role` 的既有缺陷仍未修，且 Q3=A 的測試**抓不到它**（測試斷言欄位存在，該欄位確實存在、只是值恆為空）。已在 artifact 明記此限制，避免日後有人誤以為端點測試已涵蓋該缺陷。
- 2026-08-09T13:00:00Z — **[U4]** 補丁專屬識別字的**具體字串**未定，留給實作階段。約束已寫進 artifact：須與種子的系統識別字、NULL、以及任何可能的管理員帳號互斥（欄位上限 128 字元）。
- 2026-08-09T13:00:00Z — **[U4]** 公開未認證的角色目錄端點也會呼叫種子函式，代表空表的種子行為可在啟動後被外部觸發。本次不致出錯（預設值已改為開啟，觸發結果與補丁一致），但**不在本單元的時序模型內**，已如實標為已知限制而非略過。
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-08-11T00:40:00Z — Revision 1 的分頁展開只動 U2（後端契約）與 U3（前端呈現與狀態機）兩個單元的 artifact；U1（backend-activity-policy）、U4（security-reviewer-permission）完全不受分頁影響，其功能設計一字未改 —— 這與 units-generation 的歸屬一致，不是漏改。
- 2026-08-11T00:40:00Z — 本輪未出任何新問題：分頁的每一個設計決定都能指名到上游哪一站的哪一題定案（AD-10／11/12、refined-mockups 定案 5〜9、units-generation Q4）。省題清單逐項寫進兩個單元的問題檔，而不是留白 —— 留白會讓下游以為本站漏想。
- 2026-08-11T00:40:00Z — U3 的功能設計刻意**不重述** `PaginationControl` 的六個狀態與 props（只指向 `interaction-spec.md`），改為只補三條實作層約束。理由：規格重述兩份必然漂移，而 refined-mockups 是互動規格的既定單一來源。
- 2026-08-11T02:00:00Z — reviewer iteration 1 判 NOT-READY，四項 Major 中**兩項是真的實作缺口**（不是文件瑕疵）：①C-6 的正規化契約在文件裡是硬性規則，程式碼裡完全沒有 —— 資料從 `res.json()` 直接流到元件 props；②Q1 已核可「拒絕共用工廠」而實作用了工廠。第①項已補進 `applyPage` 與啟停用的落地點；第②項判定**保留工廠**，因為 application-design 的 C-4 明文把它列為兩個合法手段之一（「不得設可靜默通過的預設值，**或**改以單一共用工廠」），Q1 是在更早、資訊較少的時點選了另一個，且其駁回理由在實作中未發生。
- 2026-08-11T02:00:00Z — 值得記住的是 reviewer 抓到①的方式：它去讀了**工作目錄裡尚未提交的程式碼**，而不是只比對文件之間是否一致。文件與文件之間當時完全自洽 —— 缺口只有在把文件對照真實實作時才看得見。這也說明「設計文件寫了某條硬性規則」與「那條規則真的存在於程式碼中」是兩件事，Construction 階段的 review 必須看後者。
- 2026-08-11T02:00:00Z — 「產生的型別已宣告必填，所以不需要執行期正規化」這個推論**是錯的**，值得記下：型別是**編譯期**保證，而 `res.json()` 回 `any`、`as UserListPage` 只是斷言。若後端映像比前端舊（`DEPLOY.md` 2.2.4 已警告的手動只重建一邊），欄位就是 undefined，型別系統完全不會知道。
- 2026-08-11T02:45:00Z — 「未受影響性查核」這個 review 形狀值得記下：把「這兩個單元不受影響」當成**待證主張**交給 reviewer，而不是預設它成立。它確認了主張為真，但**順帶查出兩個與分頁無關的實作忠實度缺口**（時區正規化函式從未被接進序列化、U4 的四態日誌契約未被遵守）—— 兩者都是「設計正確、實作沒跟上」，而只比對文件之間的一致性永遠看不到它們。
